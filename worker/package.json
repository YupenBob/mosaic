{
  "name": "mosaic-api",
  "version": "0.8.0",
  "description": "Mosaic v0.8 Worker API — auth, upload, publish, build status",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/s3-request-presigner": "^3.500.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "wrangler": "^3.100.0",
    "typescript": "^5.5.0"
  }
}
