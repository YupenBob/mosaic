{
  "name": "mosaic-cloud-admin",
  "version": "0.9.0",
  "description": "Mosaic Cloud Admin — browser-based content management",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ./",
    "deploy": "wrangler pages deploy ./"
  }
}
