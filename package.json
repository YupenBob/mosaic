{
  "name": "mosaic-ssg",
  "version": "1.0.0",
  "description": "A pure static multimedia content sharing site — like Hexo but for photo & video posts",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "node scripts/build.js --clean",
    "dev": "node scripts/build.js --watch",
    "demo": "node scripts/generate-demo.js",
    "stress": "node scripts/generate-stress-test.js"
  },
  "devDependencies": {
    "ejs": "^3.1.10",
    "fs-extra": "^11.3.0",
    "glob": "^11.0.1",
    "gray-matter": "^4.0.3",
    "html-minifier-terser": "^7.2.0",
    "marked": "^15.0.7",
    "playwright": "^1.60.0",
    "sharp": "^0.33.5"
  }
}
