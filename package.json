{
  "name": "mosaic-ssg",
  "version": "1.0.0",
  "description": "A pure static multimedia content sharing site — like Hexo but for photo & video posts",
  "type": "module",
  "scripts": {
    "compress": "node scripts/compress.js",
    "build": "node scripts/generate.js && node scripts/generate-brand-assets.js",
    "serve": "npx serve dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "ejs": "^3.1.10",
    "fs-extra": "^11.3.0",
    "glob": "^11.0.1",
    "gray-matter": "^4.0.3",
    "html-minifier-terser": "^7.2.0",
    "marked": "^15.0.7",
    "playwright": "^1.60.0",
    "sharp": "^0.33.5"
  }
}
